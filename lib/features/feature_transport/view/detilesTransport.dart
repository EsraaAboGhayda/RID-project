import 'package:flutter/material.dart';

class detilesTransport extends StatelessWidget {
  const detilesTransport({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
